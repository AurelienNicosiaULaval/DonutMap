#' DonutMap: donut maps with sf and ggplot2
#'
#' DonutMap creates donut charts positioned on a map from tidy data. The main
#' static entry point is [donut_map()]. The main interactive entry point is
#' [donut_leaflet()]. Use [donut_polygons()] when you need the `sf` polygon
#' layer directly, and [flow_lines()] when you need origin-destination line
#' geometries.
#'
#' @keywords internal
#' @importFrom rlang .data .env
"_PACKAGE"
