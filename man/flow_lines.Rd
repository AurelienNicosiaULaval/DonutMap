% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow-lines.R
\name{flow_lines}
\alias{flow_lines}
\title{Compute origin-destination flow lines}
\usage{
flow_lines(
  flows,
  locations,
  from,
  to,
  value = NULL,
  id,
  group = NULL,
  lon = NULL,
  lat = NULL,
  input_crs = 4326,
  crs = NULL,
  drop_self = TRUE,
  flow_curvature = 0,
  flow_n = 30
)
}
\arguments{
\item{flows}{A data frame containing origin-destination pairs.}

\item{locations}{A data frame or \code{sf} object containing one or more rows per
location. Repeated locations are reduced to the first geometry per \code{id}.}

\item{from, to}{Unquoted columns in \code{flows} identifying origin and
destination ids.}

\item{value}{Optional unquoted numeric column in \code{flows} used as flow value.
If omitted, each flow receives value 1.}

\item{id}{Unquoted location id column in \code{locations}.}

\item{group}{Optional unquoted column in \code{flows} used to group or colour
flow lines.}

\item{lon, lat}{Unquoted longitude and latitude columns in \code{locations}.
Required when \code{locations} is not an \code{sf} object.}

\item{input_crs}{Coordinate reference system for \code{lon} and \code{lat}, or for an
\code{sf} object with missing CRS. Defaults to EPSG:4326.}

\item{crs}{Target projected CRS. If \code{NULL}, a projected CRS is selected from
\code{locations} or an estimated UTM zone.}

\item{drop_self}{Should self-flows be removed?}

\item{flow_curvature}{Numeric curvature for trajectory lines. Use \code{0} for
straight lines, positive values for one bend direction, and negative values
for the opposite direction.}

\item{flow_n}{Number of points used to approximate each curved trajectory.}
}
\value{
An \code{sf} object with one line per retained flow.
}
\description{
\code{flow_lines()} creates \code{sf} line geometries joining origin and destination
locations. Lines can be straight or curved trajectories.
}
\examples{
locations <- data.frame(
  place = c("A", "B"),
  lon = c(-71.3, -71.1),
  lat = c(46.75, 46.85)
)
flows <- data.frame(from = "A", to = "B", trips = 15)

lines <- flow_lines(
  flows,
  locations,
  from,
  to,
  trips,
  place,
  lon = lon,
  lat = lat
)
plot(lines["value"])
}
