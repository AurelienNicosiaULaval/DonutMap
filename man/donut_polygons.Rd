% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/donut-polygons.R
\name{donut_polygons}
\alias{donut_polygons}
\title{Compute donut polygons}
\usage{
donut_polygons(
  data,
  id,
  category,
  value,
  lon = NULL,
  lat = NULL,
  input_crs = 4326,
  crs = NULL,
  map = NULL,
  radius_range = NULL,
  inner_radius = 0.55,
  n = 96,
  start_angle = pi/2
)
}
\arguments{
\item{data}{A data frame or \code{sf} object. If \code{data} is not an \code{sf} object,
\code{lon} and \code{lat} must be supplied.}

\item{id}{Unquoted column identifying each donut location.}

\item{category}{Unquoted column identifying donut categories.}

\item{value}{Unquoted numeric column giving non-negative category values.}

\item{lon, lat}{Unquoted longitude and latitude columns. Required when \code{data}
is not an \code{sf} object.}

\item{input_crs}{Coordinate reference system for \code{lon} and \code{lat}, or for an
\code{sf} object with missing CRS. Defaults to EPSG:4326.}

\item{crs}{Target projected CRS used to build the donut polygons. If \code{NULL},
a projected CRS is chosen from \code{map}, \code{data}, or an estimated UTM zone.}

\item{map}{Optional \code{sf} object used only to choose the working CRS and
default donut radius range.}

\item{radius_range}{Numeric vector of length 2 giving minimum and maximum
donut radii in map units. If \code{NULL}, a range is derived from the map extent.}

\item{inner_radius}{Numeric value in \verb{(0, 1)} giving the inner radius as a
proportion of the outer radius.}

\item{n}{Number of points used to approximate a complete outer circle.}

\item{start_angle}{Start angle in radians. The default starts at 12 o'clock.}
}
\value{
An \code{sf} object with one polygon per non-zero location-category pair.
}
\description{
\code{donut_polygons()} turns tidy category values at spatial locations into an
\code{sf} polygon layer. Each row of \code{data} represents one category for one
location.
}
\examples{
demo <- data.frame(
  place = rep(c("A", "B"), each = 3),
  lon = rep(c(-71.3, -71.1), each = 3),
  lat = rep(c(46.75, 46.85), each = 3),
  category = rep(c("x", "y", "z"), times = 2),
  value = c(10, 20, 5, 5, 15, 10)
)

donuts <- donut_polygons(demo, place, category, value, lon = lon, lat = lat)
plot(donuts["category"])
}
