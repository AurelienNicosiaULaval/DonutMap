% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/donut-leaflet.R
\name{donut_leaflet}
\alias{donut_leaflet}
\title{Draw an interactive donut map}
\usage{
donut_leaflet(
  data,
  id,
  category,
  value,
  map = NULL,
  lon = NULL,
  lat = NULL,
  input_crs = 4326,
  crs = NULL,
  radius_range = NULL,
  inner_radius = 0.55,
  n = 96,
  colours = NULL,
  flows = NULL,
  from = NULL,
  to = NULL,
  flow_value = NULL,
  flow_min = NULL,
  flow_weight_range = c(1, 8),
  flow_curvature = 0.18,
  flow_n = 30,
  flow_arrow = TRUE,
  flow_arrow_size = NULL,
  flow_colour = "grey35",
  flow_opacity = 0.55,
  provider_tiles = "CartoDB.Positron",
  popup = TRUE,
  label = TRUE,
  map_fill = "#f3f4f6",
  map_colour = "#ffffff",
  map_weight = 1,
  map_opacity = 0.9,
  donut_colour = "#ffffff",
  donut_weight = 1,
  donut_opacity = 0.9
)
}
\arguments{
\item{data}{A data frame or \code{sf} object. Each row is one category for one
donut location.}

\item{id}{Unquoted column identifying each donut location.}

\item{category}{Unquoted column identifying donut categories.}

\item{value}{Unquoted numeric column giving non-negative category values.}

\item{map}{Optional \code{sf} object used as a background layer.}

\item{lon, lat}{Unquoted longitude and latitude columns. Required when \code{data}
is not an \code{sf} object.}

\item{input_crs}{Coordinate reference system for \code{lon} and \code{lat}, or for an
\code{sf} object with missing CRS. Defaults to EPSG:4326.}

\item{crs}{Target projected CRS used to build the map.}

\item{radius_range}{Numeric vector of length 2 giving minimum and maximum
donut radii in map units. If \code{NULL}, a range is derived from the map extent.}

\item{inner_radius}{Numeric value in \verb{(0, 1)} giving the inner radius as a
proportion of the outer radius.}

\item{n}{Number of points used to approximate a complete outer circle.}

\item{colours}{Optional category colours. Use a named vector for stable
category-colour matching.}

\item{flows}{Optional data frame of origin-destination flows.}

\item{from, to}{Unquoted columns in \code{flows} identifying origin and destination
ids. Required when \code{flows} is supplied.}

\item{flow_value}{Optional unquoted numeric column in \code{flows} used to scale
line widths. If omitted, each flow receives value 1.}

\item{flow_min}{Optional minimum flow value to draw.}

\item{flow_weight_range}{Numeric vector of length 2 controlling interactive
flow line weights.}

\item{flow_curvature}{Numeric curvature for trajectory lines. Use \code{0} for
straight lines, positive values for one bend direction, and negative values
for the opposite direction.}

\item{flow_n}{Number of points used to approximate each curved trajectory.}

\item{flow_arrow}{Should interactive flow trajectories include arrowheads?}

\item{flow_arrow_size}{Arrowhead length in projected map units. If \code{NULL},
a size is derived from the donut radii.}

\item{flow_colour}{Flow line colour.}

\item{flow_opacity}{Flow line opacity.}

\item{provider_tiles}{Leaflet provider tiles. Use \code{NULL} to skip tile layers.}

\item{popup}{Should popups be attached to donut segments and flow lines?}

\item{label}{Should hover labels be attached to donut segments and flow lines?}

\item{map_fill, map_colour}{Background map fill and outline colours.}

\item{map_weight}{Background map border weight.}

\item{map_opacity}{Background map opacity.}

\item{donut_colour}{Donut segment border colour.}

\item{donut_weight}{Donut segment border weight.}

\item{donut_opacity}{Donut segment fill opacity.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
\code{donut_leaflet()} returns a \code{leaflet} htmlwidget with clickable donut
segments, optional origin-destination links or curved trajectories, popups,
labels, a legend, and layer controls.
}
\examples{
demo <- data.frame(
  place = rep(c("A", "B", "C"), each = 3),
  lon = rep(c(-71.35, -71.2, -71.05), each = 3),
  lat = rep(c(46.75, 46.82, 46.73), each = 3),
  category = rep(c("x", "y", "z"), times = 3),
  value = c(10, 20, 5, 5, 15, 10, 12, 4, 9)
)

donut_leaflet(demo, place, category, value, lon = lon, lat = lat)
}
